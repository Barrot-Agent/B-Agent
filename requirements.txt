# Barrot-Agent Python Dependencies
# For static file serving, no external packages required

# Python version constraint
python>=3.11

# Standard library only - using Python's built-in http.server module
