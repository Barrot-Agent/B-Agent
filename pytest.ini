[pytest]
markers =
    cinematic: tests for the cinematic production subsystem
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --cov=barrot_agent
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=70
    -v
