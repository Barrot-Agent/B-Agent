services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - hf-cache:/root/.cache/huggingface
    env_file:
      - .env
    environment:
      - ENVIRONMENT=development
      - DEBUG=true

volumes:
  hf-cache:
