[{"name":"list_commands","description":"Discover Orbit commands for GitLab relationship and code-structure questions. Use Orbit for callers, definitions, imports, dependency or blast-radius analysis, relationship traversal, cross-entity or cross-project joins, grouped or multi-entity aggregation, and repository maps. Do not use Orbit for a lookup, list, or simple count about one known MR, project, or user, or for writes; use ordinary GitLab tools instead. Call this before invoke_command to get command input schemas.\n\nAvailable commands:\n- query_graph: Traverse GitLab relationships and code structure for callers/imports, dependencies and blast radius, cross-entity joins, or grouped aggregation.\n- get_graph_schema: Return graph nodes, edges, and expanded node properties.\n- get_query_dsl: Return the query_graph JSON DSL grammar and version.\n- get_response_format: Return the query_graph response JSON Schema and version.","parameters":{"type":"object","properties":{"command_names":{"type":"array","items":{"type":"string"},"description":"Optional command names to describe. Omit to list every command."},"format":{"type":"string","enum":["llm","raw"],"description":"Output format. 'llm' (default) returns compact text optimized for AI. 'raw' returns structured JSON."}},"additionalProperties":false}},{"name":"invoke_command","description":"Invoke a discovered Orbit command for code structure, relationship traversal, blast radius, cross-entity or cross-project joins, or grouped aggregation. Do not use this for single-entity lookups, simple lists or counts, or writes that ordinary GitLab tools handle directly. This is a wrapper tool: keep only command_name and parameters at the top level, and put downstream command inputs inside parameters.","parameters":{"type":"object","required":["command_name"],"properties":{"command_name":{"type":"string","description":"Command name returned by list_commands."},"parameters":{"type":"object","description":"Optional downstream command input object. Put the target command inputs here, not alongside command_name."}},"additionalProperties":false}}]
